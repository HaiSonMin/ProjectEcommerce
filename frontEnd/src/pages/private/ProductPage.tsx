import { UseBrand } from "../../features/admin/brand";

const ProductPage = () => {
  // const { metadata } = UseBrand.useGetAllBrand();
  // console.log(metadata);
  return <div> Product Page</div>;
};

export default ProductPage;
