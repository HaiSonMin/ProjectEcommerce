import { styled } from "styled-components";

const StyleDarkModeToggle = styled.button``;

const DarkModeToggle = () => {
  return <StyleDarkModeToggle>Toggle DarkMode</StyleDarkModeToggle>;
};

export default DarkModeToggle;
